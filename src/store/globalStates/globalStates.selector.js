export const selectGlobalStates = (state) => state.globalStates;

export const selectTasks = (state) => state.globalStates.tasks;

export const selectElement = (state) => state.globalStates.element;

export const selectStage = (state) => state.globalStates.stage;

export const selectTaskPriority = (state) => state.globalStates.taskPriority;

export const selectNotifications = (state) => state.globalStates.notifications;

export const selectTaskToMove = (state) => state.globalStates.taskToMove;

export const selectNavOpen = (state) => state.globalStates.navOpen;

export const selectActivitySectionOpen = (state) => state.globalStates.activitySectionOpen;

export const selectIsMobile = (state) => state.globalStates.isMobile;

export const selectAdminModalOpen = (state) => state.globalStates.adminModalOpen;

export const selectUserProfileOpen = (state) => state.globalStates.userProfileOpen;

export const selectTargetUser = (state) => state.globalStates.targetUser;